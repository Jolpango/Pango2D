<StackPanel
	Id="Panel"
	Gap="10"
	Orientation="Horizontal"
	Padding="10, 75"
	Anchor="TopRight">
	<Label
		Text="{Bind:Health}, format=HP: {0}"
		BackgroundColor="rgba(255, 255, 255, 50)"
		FontSize="12"
		Opacity="0,5"
		MinSize="400, 50"
		TextAlign="Center"
		></Label>
</StackPanel>